import withLayoutBasic from "@/libs/components/layout/LayoutBasic";
import { Container } from "@mui/material";

const ProductDetail = () => {
  return <Container>PRODUCT DETAIL</Container>;
};

export default withLayoutBasic(ProductDetail);
